"""Trace local app layer: SQLite state, profile binding, and job orchestration.

The research and outbound engines stay in the existing modules
(signal_discovery, apollo_enrich, main). This package only adds the state and
safety rules the UI needs.
"""
